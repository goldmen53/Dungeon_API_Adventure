version: '3.8'
services:
  db:
    image: postgres:15
    container_name: dungeon_db
    restart: always
    environment:
      - POSTGRES_USER=player
      - POSTGRES_PASSWORD=quest
      - POSTGRES_DB=dungeon_crawler
    ports:
      - "5432:5432"